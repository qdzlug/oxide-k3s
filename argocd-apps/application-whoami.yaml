apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: whoami
  namespace: argocd
  labels:
    app: whoami
spec:
  project: default
  source:
    repoURL: 'https://github.com/qdzlug/oxide-k3s.git'
    path: argocd-apps/whoami
    targetRevision: HEAD
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: whoami
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
  revisionHistoryLimit: 3