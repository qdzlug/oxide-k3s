# ArgoCD configuration
argocd_hostname: "argocd.colo.virington.com"
argocd_tls_issuer: "letsencrypt-dns"
argocd_version: "v2.12.3"

# ArgoCD applications configuration
argocd_repo_url: "https://github.com/qdzlug/oxide-k3s.git"
argocd_repo_branch: "main"

# Demo application hostnames
demo_nginx_hostname: "demo.virington.com"
jupyter_hostname: "jupyter.virington.com"
whoami_hostname: "whoami.virington.com"
kasm_hostname: "kasm.virington.com"

# Infrastructure (nginx_lb_internal_ip derived from inventory)
dns_domain: "virington.com"
nginx_lb_external_ip: "{{ nginx_lb_ip }}"
